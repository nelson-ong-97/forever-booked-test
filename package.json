{
  "name": "upwork-be-infra",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "npx prisma migrate dev --config prisma/prisma.config.ts",
    "db:reset": "npx prisma migrate reset --config prisma/prisma.config.ts",
    "db:generate": "npx prisma generate --config prisma/prisma.config.ts",
    "poc": "npx ts-node src/create-campaign.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "prisma": "^7.5.0",
    "prisma-zod-generator": "^2.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2",
    "zod": "^4.3.6"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0",
    "dotenv": "^17.3.1"
  }
}
